import{summa, Tootaja} from './script2';
test("see test kontrollib liitmist", () => {
    expect(5 + 4).toBe(9);
});

test("see test kontrollib funktsiooni liitmist", () => {
    expect(summa(5, 4)).toBe(9);
});